
export type ColorValueProps = {
	/**
	* The current color value in hexadecimal format (e.g., "#FF5733").
	*/
	value: string;

	/**
	 * Callback function to handle value changes.
	 */
	onChange: (value: string) => void;

};
