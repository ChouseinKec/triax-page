/** Props for the HorizontalDivider component */
export type HorizontalDividerProps = {
    /** Optional title for the divider. */
    title?: string;
    /** Optional className for the divider. */
    className?: string;
    /** Visual style variation: 'dashed' or 'solid'. */
    variation?: "dashed" | "solid";
};
